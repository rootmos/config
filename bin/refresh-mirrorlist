#!/bin/bash

curl -s 'https://archlinux.org/mirrorlist/?country=DE&country=SE&protocol=https' \
    | sed 's/^#Server/Server/' \
    | rankmirrors -n 10 - \
    | sudo install --backup=numbered /dev/stdin /etc/pacman.d/mirrorlist
